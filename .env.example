# QwenProxy Configuration

# Port for the proxy server (default: 3000)
PORT=3000

# API key to protect endpoints (optional — leave empty for no auth)
API_KEY=

# Qwen credentials for automatic login
QWEN_EMAIL=
QWEN_PASSWORD=

# Browser engine: chromium (default), firefox, chrome, edge
BROWSER=chromium

# Number of concurrent chat sessions in the pool (default: 10)
POOL_SIZE=10
